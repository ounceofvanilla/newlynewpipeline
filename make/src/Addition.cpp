#include "Addition.h"

int 
Addition::twoValues(const int x, const int y)
{
    //If statement added for testing Static Code Analysis
    if (false)
    {
        char a[10];
        a[10] = 0;
        a[11] = 2;
    }
    return x + y;
}